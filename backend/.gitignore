.env
*.db
*.db-journal
*.db-wal
*.db-shm

# SSH private keys, encrypted password store, and the master key -- never commit
secrets/
!secrets/.gitkeep

# Model shards are large binaries
shards/
!shards/.gitkeep

# Generated by tools/build_web.py from ../split-inference-pipeline.html, which
# is the source of truth. Rebuild rather than commit.
web/

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# local venv used for the test run
.venv/
